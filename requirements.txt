# ThoraVis — Thoracic Pathology Classifier
# Core ML / DL
torch>=2.1.0
torchvision>=0.16.0
transformers>=4.38.0
datasets>=2.18.0,<4.0.0  # 4.0 dropped script-based dataset support; this project's
                         # HF dataset (alkzar90/NIH-Chest-X-ray-dataset) still uses one

# Computer Vision
opencv-python>=4.9.0
Pillow>=10.2.0

# Data Science
numpy>=1.26.0
scikit-learn>=1.4.0
matplotlib>=3.8.0

# Training utilities
tqdm>=4.66.0
accelerate>=0.27.0  # used internally by HuggingFace datasets for fast loading

# Notebook
jupyter>=1.0.0
ipywidgets>=8.1.0

# Inference API
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9  # required by FastAPI/Starlette for file-upload form parsing
