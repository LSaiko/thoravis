# Runtime dependencies for src/api.py only — no jupyter/notebook/matplotlib.
# See requirements.txt for the full dev environment (training, notebook,
# evaluation plots). scikit-learn/tqdm are still needed transitively:
# src/__init__.py eagerly imports train.py and evaluate.py, which use them.
torch>=2.1.0
torchvision>=0.16.0
transformers>=4.38.0
datasets>=2.18.0,<4.0.0  # src/dataset.py imports this to define PATHOLOGY_LABELS
opencv-python>=4.9.0
Pillow>=10.2.0
numpy>=1.26.0
scikit-learn>=1.4.0  # src/__init__.py eagerly imports train.py + evaluate.py, both use this
tqdm>=4.66.0         # same — pulled in transitively regardless of whether the API itself uses it
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
